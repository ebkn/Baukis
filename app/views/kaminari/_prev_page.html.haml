- if current_page.first?
  %li.page-item.disabled
    = link_to t('views.pagination.previous').html_safe, url, remote: remote, class: 'page-link'
- else
  %li.page-item
    = link_to t('views.pagination.previous').html_safe, url, remote: remote, class: 'page-link'
