- @title = '顧客アカウント編集'
%h1.title= @title

.generic_form
  = form_for @customer_form,  as: 'form', url: staff_customer_path(@customer_form.customer) do |f|
    = render 'form', f: f
    .generic_form-input
      = link_to 'キャンセル', staff_customers_path, class: 'generic_form-input-cancel'
      = f.submit '更新'
