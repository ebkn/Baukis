- @title = '顧客の新規登録'
%h1.title= @title

.generic_form
  = form_for @customer_form, as: 'form', url: staff_customers_path do |f|
    = render 'form', f: f
    .generic_form-input
      = link_to 'キャンセル', staff_customers_path, class: 'generic_form-input-cancel'
      = f.submit '登録'
