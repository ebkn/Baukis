%tr
  - unless @staff_member
    - full_name = "#{event.member.family_name} #{event.member.given_name}"
    %td= link_to full_name, admin_staff_member_staff_events_path(event.member)
  %td= event.description
  %td= event.occured_at.strftime('%Y/%m/%d %H:%M:%S')
