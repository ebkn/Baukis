- @title = '職員アカウント編集'
%h1.title= @title

.generic_form
  = form_for [:admin, @staff_member] do |f|
    = render 'form', f: f
    .generic_form-input
      = link_to 'キャンセル', admin_staff_members_path, class: 'generic_form-input-cancel'
      = f.submit '更新'
